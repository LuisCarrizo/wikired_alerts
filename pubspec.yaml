name: wikired_alerts
description: wikired_alerts is a Flutter package with lot of options of alerts with and without context.
version: 0.0.1
homepage:

environment:
  sdk: '>=3.1.0-113.0.dev <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  get: ^4.6.5
  quickalert: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0


flutter:
  uses-material-design: true